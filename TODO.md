1. Add timestamp for first purchese that client makes in sale 
- The timestamp should not be visible to the user 
- It updates only on first purchese of client in sale
2. Add client expected time of arrival 
- The expected time of arrival should be visible in the user editing screen 
- Can be set manually by the user or authomaticly by the system that will be setup in the next task
- When the time of arrival for client is set manually system should check if it already has another client with the same time and alert the user prompting to change the time or keep as is
3. Add client first order time to the client data
- The first order time should not be visible in the user editing screen 
- It updates only on first order added to the client in the sale
4. Add sale planning page.
- It should be accessible in the client selection screen from the "Więcej" panel with button named "Planowanie Sprzedaży"
- In this page user can select the dates and hours on which the sale will take place
- Sale can take multiple days, each day can have different hours
- On that place there should be button that will authomaticly generate dates for the clients expected arrival based on their order size and time of first client order
- The page should have section that has parameter that can influence the authomatic generation of clients expected arrival dates
- The parameters should consider:
- Number of products the client has ordered
- Value of products the client has ordered
